# OpenFrontBench implementation task

User approved implementation. Deliver (1) working MCP over pinned real OpenFrontIO engine, (2) pipeline launching an OpenCode playing agent through full games and suites. No evaluation LLM credentials assumed; after setup the user supplies a supported provider API key and runs the documented command. Do not stop at scaffolding.

First read ALL docs/*.md, AGENTS.md, src/, evals/, tests/, pyproject.toml. Read ~/repos/playground/openfront-tick-grid/LLM_AGENT_RESEARCH.md and relevant reference ~/repos/civ6-mcp code. Main is b2e8287; vendor pin 577819ba0e1e13ecdbc8dede2ba33de542c88a67. Leave vendor and pre-existing .opencode/metrics untouched. No git commits/push, global configuration changes or live model evaluations. Work in this checkout. Coding model stays configured default; never pass -m.

TDD is mandatory: write behaviour test, run and record expected failure, minimal implementation, run green, refactor. Keep concise real red/green command evidence in .hermes/tdd-evidence.md. No invented output. Run tests yourself. Mock only the unavailable model boundary, not game/MCP integration.

Architecture: Python MCP server using MCP SDK FastMCP; persistent Node/TS JSONL worker over pinned production core. Read production initialization/intent handling before choosing adapter. GameRunner imports a client name helper: do not assume core is dependency-free. Never default to TestConfig or omit tribes/other declared executions. Preserve upstream rules and normal bot ticks. Python owns narration, telemetry and orchestration, not duplicated game formulas.

Use paused decision boundaries: queries do not tick or consume RNG; end_decision advances 50 sim ticks. Distinguish sim time from wall-clock timeout. Use genuine small pinned plains/big_plains assets, explicitly configured populations/spawns; existing 'box-small' actually maps to 4.2M tiles. Label restricted scenario/action coverage honestly. No opponent throttling or hidden strategic assistance. Actions expand, attack target/fraction, build city at specified legal tile, pass; reject invalid/unknown inputs. Stable ordered batches and idempotency protect mutations. Production victory is separate from tick-cap/agent death/budget/infra stops.

Required pipeline: isolated per-run OpenCode configuration, MCP and versioned five-field diary/playbook. No shell/file/network tools for playing agent beyond game MCP; no source/hidden snapshot access. Consult installed CLI help/current docs, don't invent flags/event schemas. Use default/configured model, not CLI -m. Dedicated run directories, no inherited global plugins/MCO or credentials leaked in logs. Bounded calls, tokens where observable, retries and wall time; missing key must fail clearly before running. Key value only in environment/provider auth; provide one supported default provider/model configuration and documented override. Do not use existing host credentials for evaluations.

Suites: fixed seeds and paired spawn variants, repeatable manifests (engine/map/config/playbook hashes), complete tool requests/results/status/tick/decision logs, separate evaluator-only state, accepted-action replay. Store diary outside model context. Offline result/report command for individual and aggregate outcome, PMR and RAG@10. PMR excludes infrastructure and differentiates voluntary vs forced/automatic monitoring. RAG means execution fidelity (higher better), needs actual next-ten-decision evidence not mere counts. Structured commitments may support deterministic scoring but label adaptation from paper; unsupported free-text labels must be pending, not fabricated. No-diary/zero commitments N/A. No fabricated LLM performance or claim of literal numerical Civ VI reproduction.

Tests must cover real MCP stdio handshake/discovery/tools, real engine deterministic replay/state hashes across fresh processes, population/map validity, pure queries, invalid actions, timeouts/crash cleanup, diary validation, PMR categorization, RAG window boundaries, isolated OpenCode launch/config, no-key failure, labelled protocol-fixture runner, offline rescore. Exercise a full real-engine scripted game through MCP and save truthful evidence. Test doubles are explicitly NOT live LLM validation. CI should run keyless real-engine tests and installed-package entry-point smoke, not skip them all.

Use uv only for Python. Fix packaging (currently python-template), entry points and setup/doctor instructions. Lock Node dependencies and Python deps, don't mutate vendor. Final checks: uv run ruff format --check, uv run ruff check, uv run ty check, uv run pytest, uv build, TS checks/tests. README exact verified setup, keyless suite, live key setup and suite commands. First finish engine+MCP slice then runner+suite, verifying each; continue through full scope. If blocked report exact blocker rather than substitutions. Final report changed files, commands/results, artifact paths and honest remaining limitations.
